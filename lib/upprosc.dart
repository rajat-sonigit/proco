import 'package:flutter/material.dart';

class UpproSc extends StatelessWidget {
  const UpproSc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(
          child: Container(
        child: const Text("hey "),
      )),
    );
  }
}
